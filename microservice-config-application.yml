spring:
  profiles:
    # 激活开发环境配置
    active: dev
---
server:
  # 端口号
  port: 4001
spring:
  # 开发环境
  profiles: dev
  application:
    name: microservice-config-dev
---
server:
  # 端口号
  port: 4002
spring:
  # 生产环境
  profiles: prod
  application:
    name: microservice-config-prod